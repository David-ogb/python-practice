#Programme to compare 2 numbers
"""num1 = int(input("Enter the first number: "))
num2 = int(input("Enter the second number: "))

if num1 > num2:
    print(f"{num1} is the larger number")
elif num1 == num2:
    print(f"{num1} and {num2} are both equal")
else:
    print(f"{num2} is the larger number")"""


"""def get_index(x):
    try:
        print(x[1])
    except IndexError:
        print("IndexError: The list does not have an index 1.")
    finally:
        print("isjioj eifjowe fiodj f")

get_index([2])"""



"""class UsernamepasswordError(Exception):
    pass

def login(user, password):
    user_inpt = input("Enter your username: ")
    user_pass = input("Enter your password: ")
    try:
        if user_input != "David":
            raise UsernameError("Username Error!")"""



"""class TooManyAttemptError(Exception):
    pass

def ask_for_int(message, min_val, max_val, retries):
    # print a message
    try:
        for x in range(retries):
            user_input = input(message)
            if user_input >
            
    except TooManyAttemptError("That's one too many attempts")


ask_for_int(message="hello",min_val=0, max_val=9, retries=4)"""

age = null
